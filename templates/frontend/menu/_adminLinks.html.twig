{% spaceless %}
{% if 'menu' == site.adminLink
   or is_granted('ROLE_ADMIN') %}
<li>
  <a href="{{ path('admin_content_manager') }}">
    {{ 'admin.admin'|trans }}
  </a>
</li>
{% endif %}
{% if site.maintenance and is_granted('ROLE_ADMIN') %}
<li>
  <a href="{{ path('admin_site_settings') }}">
    <i id="maintenance-indicator" class="fi-flag"></i>
  </a>
</li>
{% endif %}
{% endspaceless %}
